connections:
  production:
    type: direct
    kafka:
      bootstrap_servers: "broker.confluent.cloud:9092"
      auth:
        type: api_key
        key: "mykafkakey"
        secret: "mykafkasecret"
    schema_registry:
      endpoint: "https://sr.confluent.cloud"
      auth:
        type: api_key
        key: "mysrkey"
        secret: "mysrsecret"
